-- autogenerado del csv
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
tru e
if 0 then 0 else 0 fi
if true then 0 else +0 fi
if false then false else 0 fi
¬0
¬suc(0)
¬
+
-
true ∨
∨ true
0 ∨ true
suc()
suc(0
0 = 0?
a + 0
+0 + 0
0 + +0
β(0)
β 0
β true
β(true) + +0
mín(true, 0)
mín(+0, 0)
máx(0, suc())
-- deshabilitadoHasta(restricciones): pred(0)
0 ≤ +0
negativo?(0)
|0|
Ag(true, Ag(0, ∅))
Ag(+0, Ag(0, ∅))
true ∈ Ag(0, ∅)
Ag(0, ∅) ∪ Ag(true, ∅)
π1(⟨0, true⟩) + +0
β(π1(⟨0, true⟩))
π2(⟨0, true⟩) + 0
π1(π1(⟨0, true⟩))
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- (linea que no es un caso de tipo "noparse")
-- deshabilitadoHasta(asd): true = false ∧ true --faltan parentesis
-- deshabilitadoHasta(asd): true = false = true --faltan parentesis
-- deshabilitadoHasta(asd): ¬ suc(suc(0)) < suc(0) --faltan parentesis
-- deshabilitadoHasta(asd): false ∧ true ∧ true --faltan parentesis
