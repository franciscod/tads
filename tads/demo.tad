TAD Clase

géneros clase

generadores
    crearTablero : → clase
    agregarAlumno: clase × nat → clase

otras operaciones
    estaAlumno: clase × alumno → bool

axiomas ∀ a: alumno
    estaAlumno(crearTablero(), a) ≡ false
    estaAlumno(agregarAlumno(c, a), a') ≡ a == a' ∨ estaAlumno(c, a')

Fin TAD
